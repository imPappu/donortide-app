
import React from "react";
import NewPostForm from "./post-form/NewPostForm";

// This is a simple wrapper to maintain backwards compatibility
// with existing imports in the codebase
export default NewPostForm;
