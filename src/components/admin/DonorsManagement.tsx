
import React from "react";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

const DonorsManagement = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Donors Management</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-muted-foreground">
          The donors management interface will be implemented here. This is a placeholder component.
        </p>
      </CardContent>
    </Card>
  );
};

export default DonorsManagement;
